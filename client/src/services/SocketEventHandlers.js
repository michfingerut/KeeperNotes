//TODO:

const socketEvents = [
  {
    name: 'delete group',
    action: function (store) {
      return (data) => {};
    },
  },
  {
    name: 'add member to group',
    action: function (store) {
      return (data) => {};
    },
  },
  {
    name: 'delete member from group',
    action: function (store) {
      return (data) => {};
    },
  },
  {
    name: 'post note',
    action: function (store) {
      return (data) => {};
    },
  },
  {
    name: 'delete note',
    action: function (store) {
      return (data) => {};
    },
  },
  {
    name: 'put note',
    action: function (store) {
      return (data) => {};
    },
  },
];

function initSocketEvents(store) {
  // const socket = getSocket();
  // socketEvents.forEach(({ name, action }) => socket.on(name, action(store)));
}
